query GetHotels($sortBy: SortBy) {
  hotels(sortBy: $sortBy) {
    id
    property {
      title
      rating {
        ratingValue
      }
    }
    offer {
      name
      displayPrice {
        amount
        currency
      }
    }
  }
}
